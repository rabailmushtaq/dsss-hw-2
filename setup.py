from setuptools import setup, find_packages

setup(
    name='math-quiz',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List any dependencies your project may have
        # For example, if you have numpy as a dependency, you can add it here
        # 'numpy',
    ],
)
